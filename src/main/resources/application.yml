spring:
  application:
   name: EntregaRapida

  datasource:
   url: jdbc:postgresql://localhost:5432/entregarapida
   username: ${DB_USUARIO}
   password: ${DB_SENHA}
   driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
